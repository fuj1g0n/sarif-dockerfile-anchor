version: 2

project_name: sarif-dockerfile-anchor

before:
  hooks:
    - go mod tidy

builds:
  - id: sarif-dockerfile-anchor
    main: ./cmd/sarif-dockerfile-anchor
    binary: sarif-dockerfile-anchor
    # Stamp file mtimes with the commit time for reproducible builds.
    mod_timestamp: "{{ .CommitTimestamp }}"
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - id: default
    # Asset name the composite action.yml predicts:
    #   sarif-dockerfile-anchor_<version>_<os>_<arch>.<ext>
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: ["tar.gz"]
    format_overrides:
      - goos: windows
        formats: ["zip"]

checksum:
  name_template: "checksums.txt"

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  draft: false
  prerelease: auto
